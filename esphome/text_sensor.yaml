text_sensor:
  - platform: template
    name: "${humanly_name} Uptime Readable"
    id: ${devicename}_uptime_human
    icon: mdi:clock-start

  - platform: wifi_info
    ip_address:
      name: "${humanly_name} IP Address"
    mac_address:
      name: "${humanly_name} Mac Address"

  - platform: version
    name: "${humanly_name} Version"
    hide_timestamp: true
